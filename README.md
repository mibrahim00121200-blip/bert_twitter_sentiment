# bert_twitter_sentiment
Fine-tuned bert-base-uncased on a Twitter sentiment dataset (positive/negative) with PyQt5 GUI for loading CSV datasets, loading the saved model, and predicting sentiment on selected or typed sentences. 
